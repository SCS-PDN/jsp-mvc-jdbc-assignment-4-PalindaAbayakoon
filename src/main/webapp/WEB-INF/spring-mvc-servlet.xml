<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Import JDBC config -->
    <import resource="applicationContext.xml"/>

    <!-- Scan all classes in com.university package -->
    <context:component-scan base-package="com.university"/>

    <!-- Enable @Controller, @GetMapping, etc. -->
    <mvc:annotation-driven/>

    <!-- JSP view resolver: returns "login" → /WEB-INF/views/login.jsp -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- Interceptors -->
    <mvc:interceptors>
        <!-- Logging interceptor: logs login attempts/results and course registrations -->
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="com.university.interceptor.LoggingInterceptor"/>
        </mvc:interceptor>

        <!-- Auth interceptor: redirects to /login if not logged in -->
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <mvc:exclude-mapping path="/login"/>
            <bean class="com.university.interceptor.AuthenticationInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

</beans>
